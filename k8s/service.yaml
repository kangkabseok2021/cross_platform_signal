apiVersion: v1
kind: Service
metadata:
  name: dicom-pipeline-service
  labels:
    app: dicom-pipeline
spec:
  type: ClusterIP
  selector:
    app: dicom-pipeline
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
