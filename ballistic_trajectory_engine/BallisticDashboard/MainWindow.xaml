<Window x:Class="BallisticDashboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="Ballistic Trajectory Planner" Width="1100" Height="650"
        Background="#1e1e2e">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="12"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#89b4fa"/>
            <Setter Property="Foreground" Value="#1e1e2e"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"    Value="10,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor"     Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ── Left: controls ── -->
        <StackPanel Grid.Column="0" Margin="16,20,16,16" VerticalAlignment="Top">

            <TextBlock Text="Munition Type" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ComboBox ItemsSource="{Binding MunitionTypes}"
                      SelectedItem="{Binding SelectedMunitionType}"
                      Margin="0,0,0,16" Padding="6,4"/>

            <TextBlock Text="{Binding ElevationLabel}" Margin="0,0,0,4"/>
            <Slider Minimum="1" Maximum="89"
                    Value="{Binding ElevationDeg, Mode=TwoWay}"
                    TickFrequency="10" IsSnapToTickEnabled="False"
                    Margin="0,0,0,12"/>

            <TextBlock Text="{Binding MuzzleVelocityLabel}" Margin="0,0,0,4"/>
            <Slider Minimum="100" Maximum="900"
                    Value="{Binding MuzzleVelocityMs, Mode=TwoWay}"
                    TickFrequency="100" IsSnapToTickEnabled="False"
                    Margin="0,0,0,20"/>

            <Button Content="Compute Trajectory"
                    Command="{Binding ComputeCommand}"/>

            <TextBlock Text="{Binding StatusText}"
                       Margin="0,14,0,0" TextWrapping="Wrap"
                       Foreground="#a6adc8" FontSize="11"/>
        </StackPanel>

        <!-- ── Right: OxyPlot chart ── -->
        <oxy:PlotView Grid.Column="1"
                      Model="{Binding PlotModel}"
                      Margin="0,8,8,8"
                      Background="#313244"/>
    </Grid>
</Window>
